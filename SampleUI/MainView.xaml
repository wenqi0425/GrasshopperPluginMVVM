<Window x:Class="SampleUI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SampleUI"
             mc:Ignorable="d" 
             Height="300" Width="150">
    <Window.Resources>
        
    </Window.Resources>
    <Grid Background="{Binding MyText}">
        <StackPanel Margin="10" VerticalAlignment="Center">
            <Label VerticalAlignment="Top" HorizontalAlignment="Center" Content="{Binding Counter}"/>
            <Button HorizontalAlignment="Center" VerticalAlignment="Center" Content="Click me" Command="{Binding CountUpCommand}"/>
            <TextBox Text="{Binding MyText}" Margin="0,10"/>
        </StackPanel>
    </Grid>
</Window>
